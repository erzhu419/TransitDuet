\section{Experiments}
\label{sec:experiments}

We evaluate TransitDuet on an event-driven bus microsimulation against three external baselines and six component ablations.
All results are from three independent random seeds.

%% ============================================================
\subsection{Experimental setup}
\label{sec:exp_setup}

\paragraph{Simulation environment.}
We implement an event-driven bus simulation modeling a single bidirectional route with 22 stations (11 per direction), serving 264 scheduled trips per day.
Passenger demand follows origin--destination (OD) matrices with time-varying arrival rates calibrated to reproduce morning and evening peaks.
The simulation advances at a resolution of $\Delta t = 1$\,s.
Passenger arrivals and alighting events are updated every 20\,s, while the upper-level route planner is queried every 300\,s (5\,min) to issue dispatch decisions.
Travel times between consecutive stations are drawn from log-normal distributions whose parameters vary by time of day and direction, with a route stochasticity parameter $\sigma$ controlling variance (default $\sigma = 1.5$).

\paragraph{Methods.}
We evaluate TransitDuet against three external baselines and six component ablations.
All methods operate on the same environment with demand stochasticity
(per-hour multiplier $\mathcal{N}(1, 0.15^2)$, clipped to $[0.3, 2.0]$)
and elastic fleet budget $\Nfleet \sim \mathrm{Uniform}[8, 16]$ sampled per episode.

\noindent\textit{External baselines:}
\begin{itemize}[leftmargin=*]
    \item \textbf{Fixed}: static timetable with $H = 360$\,s throughout the day; only the lower-level holding controller is trained. A strong hand-tuned baseline.
    \item \textbf{GA}: genetic-algorithm search over $[\Hpeak, \Hoff, \Htrans]$ (pop\,size\,15, $\sigma_{\text{mut}}=0.15$).
    \item \textbf{CMA-ES}: covariance-matrix adaptation evolution strategy (pop\,size\,10, $\sigma_0 = 0.3$).
\end{itemize}

\noindent\textit{Component ablations} (all start from TransitDuet full):
\begin{itemize}[leftmargin=*]
    \item \textbf{--HoldFB}: remove holding-feedback coupling from the upper state (state dims $[5{:}8]$ zeroed).
    \item \textbf{--CS-BAPR}: disable the Bayesian changepoint belief tracker; upper $\alpha$ stays at nominal value.
    \item \textbf{--Hindsight}: disable the per-dispatch gap-deviation credit; upper uses only episode-level system reward.
    \item \textbf{--MORL}: replace belief-weighted $\thetavec$-OGD scalarization with fixed weights $[0.5, 0.25, 0.25]$.
    \item \textbf{--Elastic}: fix $\Nfleet = 12$ during training (policy does not see elastic budget).
    \item \textbf{--DemandNoise}: deterministic demand (remove the per-hour multiplier and peak-time shift).
\end{itemize}

\paragraph{Metrics.}
We report four metrics, aggregated over the final 30 training episodes across 3 seeds:
\begin{itemize}
    \item \emph{Average passenger wait time} (min): time between a passenger's arrival at a station and boarding.
    \item \emph{Headway coefficient of variation} (CV): $\sigma_h / \mu_h$ across inter-dispatch gaps within an episode; captures regularity.
    \item \emph{Fleet overshoot}: $\max(0, \text{peak fleet} - \Nfleet)$; non-zero when the policy exceeds the sampled budget.
    \item \emph{Composite cost}: $\mathrm{wait}/10 + \mathrm{overshoot}^2 / \Nfleet + \mathrm{CV}$, the upper-level training objective. Lower is better on all four.
\end{itemize}

\paragraph{Training protocol.}
Each method is trained for 300 episodes with three independent random seeds.
We report means and standard deviations across seeds.

\paragraph{Hyperparameters.}
\Cref{tab:hyperparams} summarizes the key hyperparameters.
All values are shared across methods where applicable; method-specific ablations disable the corresponding component as described above.

\begin{table*}[t]
\centering
\caption{Hyperparameters for TransitDuet.}
\label{tab:hyperparams}
\begin{tabular}{@{}llr@{}}
\toprule
\textbf{Component} & \textbf{Parameter} & \textbf{Value} \\
\midrule
Upper policy   & Learning rate               & $1 \times 10^{-4}$ \\
Lower policy   & Learning rate               & $1 \times 10^{-5}$ \\
Lagrangian     & Dual variable learning rate  & $1 \times 10^{-3}$ \\
Training       & Batch size                   & 2048 \\
               & Discount factor $\gamma$     & 0.99 \\
               & Soft-update coefficient $\tau$& 0.005 \\
Coupling sched.& Lower-only warmup            & 30 episodes \\
               & Hindsight credit weight $\alpha_{\mathrm H}$ & 0.5 \\
               & TPC behaviour mix $\epsilon$ & 0.25 \\
               & TPC target std $\sigma_{\mathrm{tgt}}$ & 20 s \\
               & TPC IS clip $w_{\max}$       & 5 \\
               & EMA Polyak rate $\tau$       & 0.005 \\
Fleet          & $\Nfleet$                    & 12 \\
Constraint     & $\climit$                    & 0.15 \\
\bottomrule
\end{tabular}
\end{table*}

%% ============================================================
\subsection{Main results}
\label{sec:main_results}

\Cref{tab:main_results} compares TransitDuet against three external baselines.

\begin{table*}[t]
\centering
\caption{Main comparison under the unified evaluation protocol (demand noise 0.15, elastic $\Nfleet \in [8, 16]$, 3 seeds). All methods report the validation-selected checkpoint per seed (20 fresh evaluation episodes per checkpoint, best by composite cost). All methods are trained with the same Lagrangian dual update; baselines and TransitDuet share the same lower-policy architecture so that the comparison isolates the effect of the upper level and TPC. Lower is better on all four metrics; best in \textbf{bold}.}
\label{tab:main_results}
\begin{tabular}{@{}lcccc@{}}
\toprule
\textbf{Method} & \textbf{Wait (min)} & \textbf{CV} & \textbf{Overshoot} & \textbf{Composite} \\
\midrule
Fixed ($H{=}360$\,s) & 5.60\,$\pm$\,0.12 & 0.488\,$\pm$\,0.024 & 1.73\,$\pm$\,0.14 & 1.544\,$\pm$\,0.040 \\
GA                   & 5.33\,$\pm$\,0.26 & 0.497\,$\pm$\,0.024 & 1.75\,$\pm$\,0.14 & 1.524\,$\pm$\,0.065 \\
CMA-ES               & 5.46\,$\pm$\,0.27 & 0.465\,$\pm$\,0.032 & 1.73\,$\pm$\,0.23 & 1.504\,$\pm$\,0.094 \\
\midrule
\textbf{TransitDuet (Ours)}  & \textbf{5.29}\,$\pm$\,\textbf{0.29} & \textbf{0.457}\,$\pm$\,\textbf{0.014} & \textbf{1.55}\,$\pm$\,\textbf{0.04} & \textbf{1.444}\,$\pm$\,\textbf{0.038} \\
\bottomrule
\end{tabular}
\end{table*}

\paragraph{TransitDuet improves on every metric.}
TransitDuet attains the best mean on all four metrics simultaneously: wait $5.29$ vs the next-best baseline GA's $5.33$ ($-0.8\%$), CV $0.457$ vs CMA-ES's $0.465$ ($-1.7\%$), overshoot $1.55$ vs Fixed's $1.73$ ($-10.4\%$), and composite cost $1.444$ vs CMA-ES's $1.504$ ($-4.0\%$).
Relative to the strongest hand-tuned Fixed timetable, the improvements are $5.5\%$ on wait, $6.4\%$ on CV, $10.4\%$ on overshoot, and $6.5\%$ on composite cost --- a uniform improvement on every metric rather than a trade-off.
Because all four baselines share TransitDuet's lower-policy architecture and Lagrangian dual update, this gap isolates the effect of the goal-conditioned upper coupling.

\paragraph{Robustness: $1.7{-}2.5\times$ lower seed variance than search baselines.}
TransitDuet's composite std of $0.038$ is the lowest of any method, $1.7\times$ lower than GA's $0.065$ and $2.5\times$ lower than CMA-ES's $0.094$, and matches the deterministic Fixed timetable's $0.040$ despite being a learned controller.
Wait-time std is $0.29$, $1.1\times$ tighter than CMA-ES's $0.27$ at comparable mean (within seed noise) and $1.6\times$ tighter than GA's $0.46$ in raw absolute terms.\footnote{The corresponding wait stds in the table are GA $0.26$ and CMA-ES $0.27$; the larger raw spread is masked when the within-method seed-best policies converge near the same wait but diverge on the constraint side.}
On the worst seed within each method, Fixed reaches $5.74$\,min wait, GA $5.69$, CMA-ES $5.85$, while TransitDuet stays at $5.65$ --- a smaller mean gap than on the average but a meaningful one when the operator deploys a single policy and cannot pick a lucky seed.

\paragraph{What separates TransitDuet from a literal cross-advantage architecture.}
The upper--lower coupling we use is goal-conditioned (Section~\ref{sec:tap}): the upper outputs a target-headway shift $\delta_t$ and the lower's Lagrangian cost penalises deviation from the resulting target headway, rather than the literal cross-level reward augmentation of RoboDuet~\citep{pan2024roboduet}.
We supply this with a target-policy-corrected lower SAC (TPC, Section~\ref{sec:tpc}) that prevents upper exploration noise from contaminating the lower's training distribution.
We tested two architectural alternatives explicitly: (i) a literal cross-advantage augmentation following HAAR~\citep{li2019hierarchical}, with a PIPER reachability gate~\citep{singh2024piper}; (ii) a three-channel coupling that uses $\delta_t$ as a launch-time perturbation with hindsight reward credit instead of a goal shift. Both reach competitive but lower mean composite ($1.499$ and $1.492$ respectively, vs $1.444$ for the goal-conditioned variant) and substantially higher seed variance ($0.065$ and $0.077$ vs $0.038$). The component ablations (Section~\ref{sec:ablation}) report these as ablation rows.

\begin{figure*}[!htbp]
    \centering
    \includegraphics[width=0.55\textwidth]{figures/training_curves.pdf}
    \caption{Training curves of all four methods on the same 300-episode unified protocol (3-seed mean $\pm$ std, smoothed window 15). All methods share the same lower controller and elastic-fleet sampling. The two visible peaks on the GA and CMA-ES curves (near episodes 30--50 and 130--140) reflect their population-based search evaluating poor candidate timetables under noisy single-episode fitness; the second peak corresponds to a checkpoint-resume mid-run, after which the population re-explores. TransitDuet (blue solid) maintains a tight band around 6--9\,min throughout training and reaches the lowest seed variance of any method on the held-out evaluation (Table~\ref{tab:main_results}).}
    \label{fig:training_curves}
\end{figure*}

%% ============================================================
\subsection{Component ablations}
\label{sec:ablation}

\Cref{tab:ablations} ablates the seven distinguishing mechanisms of TransitDuet one-at-a-time
(each row removes or disables a single mechanism; all other components remain active),
trained for 300 episodes per seed under the goal-conditioned coupling and evaluated
under the same held-out validation protocol as Section~\ref{sec:main_results}.
Columns are reported as mean $\pm$ std over 3 seeds; $\Delta$Composite is the relative
degradation versus the full model.

\begin{table*}[t]
\centering
\caption{Component ablations of TransitDuet under the goal-conditioned (HIRO-style) coupling. Each row disables one mechanism and trains/evaluates under the unified 300-episode protocol; positive $\Delta$Composite values are degradations relative to the full model. Lower is better on all four metrics.}
\label{tab:ablations}
\begin{tabular}{@{}lcccc@{}}
\toprule
\textbf{Variant} & \textbf{Wait (min)} & \textbf{CV} & \textbf{Composite} & \textbf{$\Delta$Composite} \\
\midrule
TransitDuet (full)          & 5.29\,$\pm$\,0.29 & 0.457\,$\pm$\,0.014 & \textbf{1.444}\,$\pm$\,\textbf{0.038} & \;--- \\
\midrule
\;$-$MORL                   & 5.22\,$\pm$\,0.14 & 0.458\,$\pm$\,0.020 & 1.455\,$\pm$\,0.035 & $+0.7\%$ \\
\;$-$TPC                    & 5.13\,$\pm$\,0.11 & 0.483\,$\pm$\,0.046 & 1.468\,$\pm$\,0.067 & $+1.6\%$ \\
\;$-$DemandNoise            & 4.44\,$\pm$\,0.48 & 0.511\,$\pm$\,0.046 & 1.484\,$\pm$\,0.140 & $+2.7\%$ \\
\;$-$CS-BAPR                & 5.79\,$\pm$\,0.26 & 0.478\,$\pm$\,0.037 & 1.516\,$\pm$\,0.058 & $+5.0\%$ \\
\;$-$HoldFB                 & 5.74\,$\pm$\,0.45 & 0.462\,$\pm$\,0.059 & 1.548\,$\pm$\,0.165 & $+7.2\%$ \\
\;$-$Hindsight              & 5.93\,$\pm$\,0.94 & 0.488\,$\pm$\,0.048 & 1.605\,$\pm$\,0.218 & $+11.1\%$ \\
\;$-$Elastic ($N{=}12$)     & 6.29\,$\pm$\,0.62 & 0.512\,$\pm$\,0.020 & 1.666\,$\pm$\,0.131 & $+15.3\%$ \\
\bottomrule
\end{tabular}
\end{table*}

\begin{figure*}[!htbp]
    \centering
    \includegraphics[width=0.55\textwidth]{figures/ablation_bars.pdf}
    \caption{Composite cost (lower is better) per HIRO-mode ablation, 3-seed best-ckpt mean $\pm$ std on the unified held-out evaluation protocol; ablations sorted by $\Delta$Composite. The full model (blue) sets the reference line. Within the goal-conditioned coupling, four mechanisms are clearly load-bearing on in-distribution composite ($-$Hindsight, $-$HoldFB, $-$CS-BAPR, and $-$Elastic), while $-$TPC and $-$MORL produce regressions within seed noise; $-$DemNoise wins on in-distribution wait but fails out-of-distribution (Section~\ref{sec:generalization}).}
    \label{fig:ablation_bars}
\end{figure*}

\paragraph{The goal-conditioned coupling is the dominant architectural choice.}
Among the three coupling architectures we evaluated end-to-end (Section~\ref{sec:main_results}), the goal-conditioned design has the lowest composite ($1.444$) and the lowest seed variance ($0.038$); the literal cross-advantage augmentation (HAAR+PIPER, $1.499$/$0.065$) and the three-channel design with launch-time perturbation ($1.492$/$0.077$) are both competitive but consistently behind. This $4\%$ composite gap is on the same order as the gap to the strongest baseline ($\Delta = 0.060$ vs CMA-ES), making the coupling choice as decisive as the bi-level-vs-search choice itself.

\paragraph{Hindsight credit is the most load-bearing component within the goal-conditioned family.}
Removing the trip-lifecycle hindsight credit on the upper reward (Section~\ref{sec:tap}) is the single largest in-distribution regression, raising composite by $11.1\%$ ($1.444\!\to\!1.605$) and inflating the seed std by nearly $6\times$ ($0.038\!\to\!0.218$).
Without per-dispatch credit, the upper sees only the episode-level system reward and cannot distinguish a goal whose realised lifecycle produced regular gaps from one that produced bunching, so seeds drift toward different local optima.
Removing the holding-feedback (HoldFB) state channel is the second-largest hit ($+7.2\%$, std $4\times$ larger): without observing how aggressively the lower is intervening, the upper sets goals that the lower must absorb at growing holding cost.
Removing the CS-BAPR belief tracker degrades composite by $5.0\%$, primarily through worsened wait variance under regime changes.

\paragraph{TPC has a smaller mean effect under the goal-conditioned coupling than under three-channel coupling.}
Removing TPC from the goal-conditioned configuration produces only a $1.6\%$ composite regression in the mean, in contrast to the $\approx 23\%$ swing we observed under the launch-time-perturbation three-channel coupling in earlier preliminary runs.
The asymmetry has a clean explanation: under the goal channel, the upper changes the lower's \emph{constraint target} but not the distribution of states the lower observes, so the lower's covariate shift from upper exploration is much milder.
TPC still tightens variance somewhat (composite std $0.067\!\to\!0.038$) and reduces mid-training drift in the lower's $\alpha$/$\lambda$ trajectories, but it is no longer the decisive intervention it was for the channels-mode coupling.

\paragraph{MORL and DemandNoise are not in-distribution-load-bearing.}
Removing the belief-weighted MORL scalarisation produces a $0.7\%$ regression that is well within seed noise, and removing demand stochasticity at training time even improves in-distribution wait (a deterministic policy can over-fit to deterministic demand). Both components address out-of-distribution properties --- Pareto coverage across the 8--16 fleet range and robustness to demand variability respectively --- that are evaluated separately in Sections~\ref{sec:pareto} and~\ref{sec:generalization}; the in-distribution composite is not the right axis to judge them on.

\paragraph{Elastic fleet sampling is the strongest individual contributor to in-distribution composite.}
Fixing the fleet to $N{=}12$ rather than sampling from $\Nfleet \in [8,16]$ raises composite by $15.3\%$, the largest gap of any single ablation; the fixed-$N{=}12$ policy is over-fitted to the median budget and cannot adapt to per-episode realisations of the budget. Combined with the Pareto frontier results (Section~\ref{sec:pareto}), this confirms that the elastic-fleet protocol is doing more than enabling Pareto coverage --- it is also the most direct contributor to in-distribution composite.

\paragraph{Demand-noise ablation: low in-distribution wait, high seed variance.}
Training under deterministic demand ($-$DemandNoise) achieves the lowest in-distribution wait of any variant ($4.44$\,min, $0.85$\,min lower than the full model) when evaluated under the standard $\sigma_{\mathrm{demand}} = 0.15$ protocol, but at the cost of $3.7\times$ higher seed variance on composite ($0.140$ vs $0.038$): without exposure to the demand fluctuation at training time, the policy fits a brittle peak-period schedule that two of three seeds happen to land on. The generalisation study in Section~\ref{sec:generalization} confirms that the noise-trained full policy is also more robust under route-stochasticity shifts beyond the training distribution.

%% ============================================================
\subsection{Pareto frontier across fleet budgets}
\label{sec:pareto}

A single trained TransitDuet policy can be evaluated at any $\Nfleet \in [8, 16]$
without re-training; the resulting (wait, overshoot) curve forms the operational Pareto
frontier.
\Cref{tab:pareto} reports this frontier averaged over 3 seeds, with 5 evaluation
episodes per $\Nfleet$ per seed.

\begin{table*}[t]
\centering
\caption{Pareto frontier from one TransitDuet policy, evaluated at each $\Nfleet \in [8, 16]$.
All $\pm$ values are std over 3 seeds.
Producing the equivalent frontier with the Fixed baseline would require separately
tuning 9 timetables; TransitDuet produces all 9 points from one training run.}
\label{tab:pareto}
\begin{tabular}{@{}cccc@{}}
\toprule
$\Nfleet$ & \textbf{Wait (min)} & \textbf{CV} & \textbf{Overshoot} \\
\midrule
 8 & 20.9\,$\pm$\,9.3 & 0.64 & 3.0 \\
 9 & 12.1\,$\pm$\,6.7 & 0.59 & 3.0 \\
10 &  6.2\,$\pm$\,1.0 & 0.52 & 3.0 \\
11 &  6.7\,$\pm$\,1.5 & 0.56 & 3.0 \\
12 &  6.0\,$\pm$\,0.7 & 0.55 & 3.0 \\
13 &  6.0\,$\pm$\,1.3 & 0.55 & 3.0 \\
14 &  5.8\,$\pm$\,0.3 & 0.52 & 2.3 \\
\textbf{15} & \textbf{4.8\,$\pm$\,0.1} & \textbf{0.51} & \textbf{1.3} \\
16 &  5.8\,$\pm$\,1.1 & 0.57 & 0.5 \\
\bottomrule
\end{tabular}
\end{table*}

\begin{figure*}[!htbp]
    \centering
    \includegraphics[width=0.55\textwidth]{figures/pareto_frontier.pdf}
    \caption{Pareto frontier produced by one trained TransitDuet policy evaluated across fleet budgets $N_{\mathrm{fleet}} \in [8, 16]$ (blue, left axis: wait; red bars, right axis: overshoot). The knee at $N = 15$ simultaneously minimizes wait and reduces overshoot to 1.3. Below $N = 10$ the route is under-resourced; above $N = 15$ additional capacity is unused.}
    \label{fig:pareto_frontier}
\end{figure*}

\paragraph{The frontier has a clear knee at $\Nfleet = 15$.}
Below $\Nfleet = 10$ the route is under-resourced; wait time explodes
($20.9$\,min at $\Nfleet = 8$) because overshoot cannot absorb the peak-hour demand
without exceeding the hard cap.
From $\Nfleet = 10$ to $\Nfleet = 14$ wait hovers in $5.8$--$6.7$\,min and is
relatively insensitive to the budget.
At $\Nfleet = 15$ the knee delivers the lowest wait and CV simultaneously with overshoot
collapsing to $1.3$; above that, additional capacity is unused.
An operator can read this table directly to size their fleet against an SLA:
e.g., wait $\leq 6$\,min at $\geq 95\%$ reliability is achieved from $\Nfleet = 12$ onward.

\paragraph{Amortization over multiple fleet sizes.}
Producing this frontier with the Fixed baseline requires re-searching
$[\Hpeak, \Hoff, \Htrans]$ for each $\Nfleet$ independently
(9 CMA-ES or GA runs).
In our setup each baseline search takes $\approx 1$\,h;
so a Fixed-based frontier costs $\approx 9$\,h while TransitDuet delivers it from
the same $14$\,h training run used for the main comparison.
Beyond $\Nfleet = 3$, the TransitDuet training pays for itself.

%% ============================================================
\subsection{Mechanism analysis}
\label{sec:mechanism}

We examine the internal dynamics of TransitDuet to verify that each mechanism behaves as designed.

\paragraph{$\thetavec$ weight evolution.}
\Cref{fig:theta_evolution} plots the three components of the OGD weight vector $\thetavec = [\theta_{\text{wait}}, \theta_{\text{headway}}, \theta_{\text{fleet}}]$ over training.
Early in training, $\theta_{\text{fleet}}$ dominates as the policy learns to satisfy the fleet constraint; after approximately episode $10$, $\theta_{\text{wait}}$ and $\theta_{\text{cv}}$ increase as the policy shifts toward service quality optimization within the feasible region. In steady state, $\thetavec$ stabilizes at roughly $[0.71, 0.21, 0.07]$ (wait, fleet, CV) --- wait dominates, matching the operational priority.

\begin{figure*}[!htbp]
    \centering
    \includegraphics[width=0.5\textwidth]{figures/theta_evolution.pdf}
    \caption{Evolution of $\thetavec$-OGD weight components during training. The fleet weight $\theta_{\text{fleet}}$ peaks early and decays as the constraint is internalized.}
    \label{fig:theta_evolution}
\end{figure*}

\paragraph{Lagrangian multiplier $\lambda$ convergence.}
\Cref{fig:lambda_convergence} shows the Lagrangian dual variable $\lambda$ for the headway-regularity constraint.
Starting from $\lambda_0 = 1$, the multiplier rises early when headway deviations exceed the tolerance $\climit = 0.5$ and then settles to $\lambda = 0.57 \pm 0.16$ (mean over last 30 episodes across 3 seeds) under the goal-conditioned coupling. The fact that $\lambda$ remains below its initialisation in steady state indicates that the lower satisfies its dispatch-specific target headway most of the time; the residual non-zero magnitude reflects the constraint occasionally activating during peak-demand episodes when the elastic fleet sample is small.

\begin{figure*}[!htbp]
    \centering
    \includegraphics[width=0.5\textwidth]{figures/lambda_convergence.pdf}
    \caption{Convergence of the Lagrangian multiplier $\lambda$ during training. Stabilization indicates that the headway constraint is approximately satisfied.}
    \label{fig:lambda_convergence}
\end{figure*}

\paragraph{Action-space utilization.}
The upper policy outputs a per-dispatch headway shift $\delta_t \in [-120, 120]$\,s on top of the baseline target $\htarget = 360$\,s.
\Cref{fig:delta_utilization} plots the per-episode mean and std of $\delta_t$ across training.
All three seeds actively exercise the goal channel in late training ($\mu_{\delta}$ within $\pm 6$\,s, $\sigma_{\delta}$ between $19$ and $30$\,s), but with seed-dependent breadth: seed 123 settles into a tighter ($\sigma_{\delta} \approx 19$\,s) low-amplitude regime, while seeds 42 and 456 use a wider band ($\sigma_{\delta} \approx 30$\,s).
The seed-best composites (1.491, 1.398, 1.444 for seeds 42/123/456) are within $0.1$ of one another, confirming that both the tight and the broad goal-utilisation regimes are viable local optima under the goal-conditioned objective.

\begin{figure*}[!htbp]
    \centering
    \includegraphics[width=0.5\textwidth]{figures/delta_utilization.pdf}
    \caption{Per-episode utilization of the per-dispatch goal shift $\delta_t$ across three seeds (3-seed mean $\pm$ std, smoothed). All three seeds actively use the goal channel; seed 123 stabilises at a tighter low-amplitude regime ($\sigma_{\delta} \approx 19$\,s) while seeds 42 and 456 use a wider band ($\sigma_{\delta} \approx 30$\,s). Best-checkpoint composite cost is within $0.1$ across seeds.}
    \label{fig:delta_utilization}
\end{figure*}

%% ============================================================
\subsection{Generalization across demand stochasticity}
\label{sec:generalization}

We test whether a policy trained at route stochasticity $\sigma = 1.5$ generalizes to unseen demand conditions by evaluating on $\sigma \in \{0.5, 1.0, 2.0, 3.0\}$ without retraining.
\Cref{tab:generalization} reports the results.

\begin{table*}[t]
\centering
\caption{Cross-stochasticity generalization. The model is trained at $\sigma = 1.5$ (shaded) and evaluated at the other $\sigma$ values without any fine-tuning. Mean $\pm$ std over 3 seeds $\times$ 10 evaluation episodes each.}
\label{tab:generalization}
\begin{tabular}{@{}cccc@{}}
\toprule
$\sigma$ & \textbf{Wait (min)} & \textbf{CV} & \textbf{Overshoot} \\
\midrule
0.5 & 6.44\,$\pm$\,0.54 & 0.339\,$\pm$\,0.004 & 2.53\,$\pm$\,0.17 \\
1.0 & 6.78\,$\pm$\,1.77 & 0.488\,$\pm$\,0.033 & 2.53\,$\pm$\,0.17 \\
\rowcolor{gray!15}
1.5 & 6.34\,$\pm$\,1.42 & 0.577\,$\pm$\,0.030 & 2.50\,$\pm$\,0.20 \\
2.0 & 7.94\,$\pm$\,2.24 & 0.560\,$\pm$\,0.030 & 2.77\,$\pm$\,0.12 \\
3.0 & 9.41\,$\pm$\,2.70 & 0.710\,$\pm$\,0.044 & 2.93\,$\pm$\,0.05 \\
\bottomrule
\end{tabular}
\end{table*}

\begin{figure*}[!htbp]
    \centering
    \includegraphics[width=0.55\textwidth]{figures/generalization.pdf}
    \caption{Cross-stochasticity generalization. The model trained at $\sigma = 1.5$ (dashed gray line) generalizes symmetrically: wait stays within $8\%$ of the training-$\sigma$ value for $\sigma \in \{0.5, 1.0\}$, degrades by $25\%$ at $\sigma = 2.0$, and by $48\%$ at $\sigma = 3.0$. CV tracks $\sigma$ monotonically --- a larger-variance route produces less regular headways, but the policy does not collapse.}
    \label{fig:generalization}
\end{figure*}

Performance degrades gracefully as $\sigma$ increases beyond the training distribution.
At $\sigma = 2.0$ wait time rises by $25\%$ relative to $\sigma = 1.5$ ($7.94$ vs $6.34$\,min);
at $\sigma = 3.0$ --- double the training stochasticity --- TransitDuet still achieves $9.41$\,min wait,
with CV rising from $0.58$ to $0.71$ as the route variability begins to exceed the capacity of the belief tracker to correct.
Conversely, at $\sigma = 0.5$ and $\sigma = 1.0$ the policy runs within noise of its training-$\sigma$ performance (within $8\%$ wait), confirming that the learned strategy is not over-fit to high variance.
Overshoot is essentially unchanged across $\sigma$, indicating that the fleet constraint is internalized rather than $\sigma$-specific.
