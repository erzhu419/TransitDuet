_extends: T_freqhrl_terminal.yaml
_name: T_swapped_terminal

frequency:
  upper_mode: high
  lower_mode: low
  promotion:
    enable: true
    window_min: 10.0
    cooldown_min: 10.0
    residual_threshold: 0.85
    persistence_ratio: 0.35

leakage:
  enable: false
