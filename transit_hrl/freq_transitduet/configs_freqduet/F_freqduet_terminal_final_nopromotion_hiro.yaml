_extends: F_freqduet_terminal_lowerhf_localcap10_w06_hiro.yaml
_name: F_freqduet_terminal_final_nopromotion_hiro

frequency:
  # Explicit no-promotion baseline now that main includes trigger-only
  # cross-frequency promotion.
  promotion:
    enable: false
