_extends: F_freqduet_terminal_hiro.yaml
_name: F_freqduet_terminal_swapped_hiro

frequency:
  # Deliberately wrong allocation: upper gets high-frequency residual state,
  # lower gets low-frequency demand state.
  upper_mode: high
  lower_mode: low
