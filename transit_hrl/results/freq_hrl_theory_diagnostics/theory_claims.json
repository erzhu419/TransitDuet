{
  "theorems": [
    {
      "id": "T1",
      "title": "Causal Encoder Nonanticipativity",
      "statement": "For any time t, frequency features z_t produced by a CausalSpectralEncoder are measurable with respect to the sigma-field generated by bins observed at times <= t.",
      "assumptions": [
        "The domain adapter only passes bins to encoder.update after their event time.",
        "Encoder state transitions read only the previous encoder state and the current bin.",
        "Feature calls do not run offline smoothing over future bins."
      ],
      "proof": "Induct on update count. The initial state is deterministic. If the state before update k is a function only of bins 1..k-1, the next state is a deterministic function of that state and bin k. Therefore every feature emitted after update k is a function only of bins 1..k.",
      "diagnostic": "test_causality covers EMA, Fourier, state-space, wavelet, adaptive wavelet, and neural/PINN state-space prefixes."
    },
    {
      "id": "T2",
      "title": "Leakage-Shaped Return Lower Bound",
      "statement": "If leakage L_t >= 0 and shaped reward is r'_t = r_t - lambda L_t with lambda >= 0, then shaped episodic return is a conservative lower bound on task return up to lambda sum_t L_t.",
      "assumptions": [
        "Leakage is computed causally from upper and lower action effects.",
        "The leakage multiplier is nonnegative.",
        "The task return is the unshaped sum of r_t on the same trajectory."
      ],
      "proof": "Summing r'_t gives sum_t r_t - lambda sum_t L_t. Since lambda >= 0 and L_t >= 0, shaped return cannot exceed task return. The exact gap is lambda sum_t L_t.",
      "diagnostic": "paper statistical checks report lower-LF drift and no-tradeoff gates for Trading and Transit constraints."
    },
    {
      "id": "T3",
      "title": "Primal-Dual Leakage Constraint Direction",
      "statement": "For a fixed policy class and positive dual step size, the primal-dual update increases the leakage multiplier when observed constraint cost exceeds its budget and decreases it when the cost is below budget.",
      "assumptions": [
        "Constraint samples are finite and computed on the current rollout.",
        "The dual variable is projected onto a nonnegative bounded interval.",
        "The actor update includes the current multiplier times constraint excess."
      ],
      "proof": "The dual update is projected gradient ascent on eta E[c_t - budget]. A positive excess gives a positive gradient for eta; a negative excess gives a negative gradient. Projection preserves nonnegativity and boundedness.",
      "diagnostic": "DualActorCriticPPO tests plus trading/transit primal-dual validation check the expected drift direction."
    },
    {
      "id": "T4",
      "title": "Promotion Threshold Tradeoff",
      "statement": "For a persistent residual detector with threshold tau and persistence ratio rho, increasing tau or rho weakly reduces stationary false positives but can increase detection delay and false negatives after true persistent shocks.",
      "assumptions": [
        "Promotion uses only residual magnitudes in a finite causal window.",
        "False positive means the window statistic crosses the gate in a stationary regime.",
        "True shock means the residual distribution shifts upward for multiple bins."
      ],
      "proof": "The promotion event is a monotone threshold event in residual exceedance counts. Raising tau or rho can only remove crossing windows under the same residual path. That reduces stationary crossings, but it can also delay or remove crossings on a shifted path whose exceedance count is near the old threshold.",
      "diagnostic": "promotion sweep, recovery validation, and learned Transit promotion-replan checks should be reported together."
    },
    {
      "id": "T5",
      "title": "Hierarchical Wait-Credit Residual Bound",
      "statement": "If the passenger-wait credit c_t is decomposed into upper and lower frequency credits c_t^U and c_t^L on the same causal rollout, then the episode-level attribution error is bounded by sum_t |c_t - c_t^U - c_t^L|.",
      "assumptions": [
        "Upper, lower, and total wait credits are computed from the same observed rollout.",
        "The policy loss consumes only causal credit terms available at time t.",
        "The residual is logged by the validation harness or can be reconstructed from logs."
      ],
      "proof": "For each time step, the absolute attribution mismatch is |c_t - c_t^U - c_t^L|. Summing over the episode and applying the triangle inequality gives the stated L1 residual bound. Thus a small logged residual certifies that frequency-attributed policy losses are close to the intended passenger-wait objective.",
      "diagnostic": "native wait-credit and real-demand control checks report wait/reward deltas; future native OD logs should add explicit credit-residual columns."
    },
    {
      "id": "T6",
      "title": "Paired Validation CI Width",
      "statement": "For paired seed/source deltas with empirical standard deviation s and n independent pairs, the normal-approximation confidence half-width decreases as z s / sqrt(n).",
      "assumptions": [
        "Treatment and control are paired by seed/source window.",
        "Paired deltas have finite variance.",
        "The reported z value matches the desired two-sided confidence level."
      ],
      "proof": "The paired estimator is the sample mean of deltas. Its standard error is s / sqrt(n). Multiplying by the normal critical value z gives the usual large-sample half-width. This quantifies why native promotion and real-demand controls need more paired seeds when reward/wait intervals cross zero.",
      "diagnostic": "paper statistical checks expose n_common and CI bounds for every paired claim."
    }
  ],
  "statistical_coverage": {
    "n_checks": 59,
    "supported_or_positive": 38,
    "min_pairs": 3,
    "median_pairs": 12.0,
    "max_pairs": 89
  },
  "paper_diagnostics_path": "transit_hrl/results/freq_hrl_paper_diagnostics/claim_matrix.json"
}