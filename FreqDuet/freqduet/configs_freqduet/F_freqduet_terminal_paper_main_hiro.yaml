_extends: F_freqduet_terminal_main_cfaction_domainbest_v1_hiro.yaml
_name: F_freqduet_terminal_paper_main_hiro

# Paper-main alias promoted from the deterministic cfaction_domainbest_v1
# candidate. The old F_freqduet_terminal_main_hiro remains available as the
# current-main baseline to avoid a cyclic config inheritance chain.
