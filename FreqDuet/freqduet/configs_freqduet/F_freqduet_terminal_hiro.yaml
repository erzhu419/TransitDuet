_extends: F_freqduet_timetable_hiro.yaml
_name: F_freqduet_terminal_hiro

upper:
  # Experimental terminal-dispatch variant. Unlike the default timetable
  # config, this writes executable scheduled launch times.
  timetable_planner:
    terminal_dispatch: true
    terminal_shift_min_s: -120.0
    terminal_shift_max_s: 60.0
