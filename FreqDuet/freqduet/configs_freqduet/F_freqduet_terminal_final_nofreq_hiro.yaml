_extends: F_freqduet_terminal_final_nopromotion_hiro.yaml
_name: F_freqduet_terminal_final_nofreq_hiro

upper:
  state_dim: 11

frequency:
  enable: false

reward_attribution:
  # No frequency split exists in this baseline, so do not apply
  # frequency-attributed wait shaping.
  enable: false
