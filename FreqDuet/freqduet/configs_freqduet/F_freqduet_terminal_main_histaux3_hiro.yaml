_extends: F_freqduet_terminal_main_hiro.yaml
_name: F_freqduet_terminal_main_histaux3_hiro

# Candidate only: keep the promoted harmonic LF/HF split and append a short
# causal realized-demand residual memory. This tests whether rawhistory's broad
# scenario advantage comes from short memory rather than replacing the
# historical-prior decomposer.
frequency:
  history_aux:
    enable: true
    mode: residual
    upper_bins: 3
    lower_bins: 3
    clip: 2.0
