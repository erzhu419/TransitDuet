_extends: F_freqduet_terminal_main_hiro.yaml
_name: F_freqduet_terminal_main_release10_hiro

# Phase-4 executable terminal-dispatch candidate: allow limited early release
# for future trips when the upper timetable compresses headway.
upper:
  timetable_planner:
    terminal_dispatch: true
    terminal_shift_min_s: -10.0
    terminal_shift_max_s: 45.0
