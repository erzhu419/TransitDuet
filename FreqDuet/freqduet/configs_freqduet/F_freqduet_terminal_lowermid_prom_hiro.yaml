_extends: F_freqduet_terminal_main_hiro.yaml
_name: F_freqduet_terminal_lowermid_prom_hiro

# Lower still sees HF residuals, but also receives causal middle-band regime
# confidence. This tests whether highnoise failures are caused by treating
# one-step noise and sustained local regime pressure as the same signal.
frequency:
  lower_mode: high_mid
