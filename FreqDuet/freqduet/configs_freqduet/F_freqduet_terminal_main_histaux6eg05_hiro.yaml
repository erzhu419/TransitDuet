_extends: F_freqduet_terminal_main_hiro.yaml
_name: F_freqduet_terminal_main_histaux6eg05_hiro

# Candidate only: harmonic LF/HF split plus 6-bin causal residual memory,
# disabled smoothly when global HF energy indicates strong demand noise.
frequency:
  history_aux:
    enable: true
    mode: residual
    upper_bins: 6
    lower_bins: 6
    clip: 2.0
    high_energy_max: 0.05
    high_energy_width: 0.02
    min_weight: 0.0
