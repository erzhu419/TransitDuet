_extends: F_freqduet_terminal_main_hiro.yaml
_name: F_freqduet_terminal_main_snapshotodtermcap_hiro

# Cap the value-selected positive terminal bias in the stable terminal regime.
# Highnoise and rushshift keep current main behavior in their domain configs.
upper:
  snapshot_value_selector:
    allowed_methods:
      - actor_term45_0
      - actor_target_p15
      - actor_term45_p15
