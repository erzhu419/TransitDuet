_extends: F_freqduet_terminal_main_hiro.yaml
_name: F_freqduet_terminal_main_histaux6eg06upper_hiro

# Candidate only: keep gated harmonic residual memory in the upper timetable
# state, but do not expose it to the lower holding policy.
frequency:
  history_aux:
    enable: true
    mode: residual
    upper_bins: 6
    lower_bins: 0
    clip: 2.0
    high_energy_max: 0.06
    high_energy_width: 0.02
    min_weight: 0.0
