_extends: F_freqduet_timetable_hiro.yaml
_name: F_freqduet_terminal_hold60_hiro

upper:
  # Same no-early-launch terminal timetable with a wider terminal holding cap.
  timetable_planner:
    terminal_dispatch: true
    terminal_shift_min_s: 0.0
    terminal_shift_max_s: 60.0
