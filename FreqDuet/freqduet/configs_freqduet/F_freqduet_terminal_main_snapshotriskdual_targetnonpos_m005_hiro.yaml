_extends: F_freqduet_terminal_main_snapshotriskdual_targetonly_m005_hiro.yaml
_name: F_freqduet_terminal_main_snapshotriskdual_targetnonpos_m005_hiro

# Positive target offsets dominate the unsafe target-only override and
# reintroduce overshoot. Keep only nonpositive target candidates as a discrete
# action-space shrink.
upper:
  snapshot_action_value_selector:
    allowed_methods:
      - actor_target_m30
      - actor_target_m15
      - actor_target_0
