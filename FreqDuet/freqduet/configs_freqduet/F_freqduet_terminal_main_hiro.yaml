_extends: F_freqduet_terminal_aligned_nopromotion_hiro.yaml
_name: F_freqduet_terminal_main_hiro

# Current promoted FreqDuet main path after the 40-episode aligned baseline
# matrix. Conservative promotion remains implemented and tested, but is not the
# default because it degraded composite under the longer protocol.
